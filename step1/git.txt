git status 상태확인

깃 저장
git add .
git commit -m "작업 내용 저장"
git push origin main


새로운 컴퓨터에서 SSH 키를 생성하고 깃허브에 등록하는 과정을 딱 3단계로 정리해 드릴게요. 터미널(또는 CMD, PowerShell)을 열고 따라 하시면 됩니다.

1단계: 내 컴퓨터에서 SSH 키 생성하기

터미널에 아래 명령어를 입력하세요. (이메일 주소는 본인 깃허브 계정 이메일로 바꾸세요.)

Bash
ssh-keygen -t ed25519 -C "dahan9819@skuniv.ac.kr"
ssh -T git@github.com
ls -al ~/.ssh